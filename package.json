{
  "name": "taxly-api",
  "version": "0.1.0",
  "description": "A Node.js wrapper for the Taxly REST API",
  "main": "build/index.js",
  "scripts": {
    "build": "babel src -d build",
    "prepublish": "babel src -d build",
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "author": "Michal Kowalkowski",
  "license": "MIT",
  "dependencies": {
    "babel-polyfill": "^6.26.0",
    "request": "^2.87.0"
  },
  "devDependencies": {
    "babel-cli": "^6.26.0",
    "babel-core": "^6.26.3",
    "babel-preset-env": "^1.7.0"
  }
}
